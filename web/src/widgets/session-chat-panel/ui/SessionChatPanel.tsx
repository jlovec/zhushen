/**
 * @deprecated 请直接使用 `@/components/SessionChat`。
 * 该别名仅为兼容旧导出保留，不再承载独立实现。
 */
export { SessionChat as SessionChatPanel } from '@/components/SessionChat'
export type { SessionChatProps as SessionChatPanelProps } from '@/components/SessionChat'
